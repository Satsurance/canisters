{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "icp_canister",
  "scripts": {
    "prebuild": "cd src/sat-ui && npm install",
    "build": "cd src/sat-ui && npm run build",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/icp_canister_frontend"
  ]
}